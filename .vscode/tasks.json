{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "run all tests",
            "type": "shell",
            "command": "pixi run --environment test pytest ./src/even_flow/tests"
        },
        {
            "label": "run fast tests",
            "type": "shell",
            "command": "pixi run --environment test pytest -m fast ./src/even_flow/tests"
        },
        {
            "label": "run detailed tests",
            "type": "shell",
            "command": "pixi run --environment test pytest -m detailed ./src/even_flow/tests"
        },
        {
            "label": "run demos",
            "type": "shell",
            "command": "pixi run --environment test pytest -m demos ./src/even_flow/tests"
        }
    ]
}